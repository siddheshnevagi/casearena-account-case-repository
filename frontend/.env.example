# Copy to .env.local for local development.
#
# Currently pointed at the friendly-happiness-4qqj5r7pj4wvf55gg Codespace's
# forwarded backend port for live testing there (a browser outside the
# Codespace can't reach it via localhost:8000) — swap back to
# http://localhost:8000 once that Codespace's session is done, or for any
# other environment/Codespace (the forwarded hostname is unique per
# Codespace instance).
VITE_API_BASE_URL=https://friendly-happiness-4qqj5r7pj4wvf55gg-8000.app.github.dev
